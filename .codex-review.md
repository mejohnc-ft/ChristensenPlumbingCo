# Code Review Request

Please review the following React/TypeScript code for a plumbing company website project. Focus on:

1. **Security vulnerabilities** (XSS, SQL injection, auth issues, data exposure)
2. **Code quality** (best practices, TypeScript usage, React patterns)
3. **Performance issues** (rendering, state management, memory leaks)
4. **Type safety** (any issues, proper typing)
5. **Potential bugs** (logic errors, edge cases, race conditions)

## Files to Review

### 1. src/lib/supabase.ts
```typescript
import { createClient, SupabaseClient } from '@supabase/supabase-js';

const supabaseUrl = import.meta.env.VITE_SUPABASE_URL;
const supabaseAnonKey = import.meta.env.VITE_SUPABASE_ANON_KEY;

// Create a mock client for development when env vars are missing
const createMockClient = () => ({
  auth: {
    getSession: () => Promise.resolve({ data: { session: null }, error: null }),
    onAuthStateChange: () => ({ data: { subscription: { unsubscribe: () => {} } } }),
    signOut: () => Promise.resolve({ error: null }),
    signInWithPassword: () => Promise.resolve({ data: { session: null }, error: { message: 'Supabase not configured' } }),
  },
  from: () => ({
    select: () => ({ data: [], error: null, order: () => ({ data: [], error: null }) }),
    insert: () => ({ data: null, error: { message: 'Supabase not configured' } }),
    update: () => ({ data: null, error: { message: 'Supabase not configured' } }),
    delete: () => ({ data: null, error: { message: 'Supabase not configured' } }),
  }),
  storage: {
    from: () => ({
      upload: () => Promise.resolve({ data: null, error: { message: 'Supabase not configured' } }),
      getPublicUrl: () => ({ data: { publicUrl: '' } }),
    }),
  },
});

export const supabase: SupabaseClient | ReturnType<typeof createMockClient> =
  supabaseUrl && supabaseAnonKey
    ? createClient(supabaseUrl, supabaseAnonKey)
    : createMockClient() as unknown as SupabaseClient;

export const isSupabaseConfigured = !!(supabaseUrl && supabaseAnonKey);
```

### 2. src/components/ProjectCarousel.tsx
- 175 lines of React component code with carousel functionality
- Auto-play feature with timeouts
- Image rendering from external URLs
- Featured project badges

### 3. src/pages/PhotoUpload.tsx
- 1236 lines of complex admin interface
- File upload handling (drag & drop, multi-file)
- Authentication with Supabase
- CRUD operations for projects and photos
- Hardcoded email: 'mejohnc@christensenplumbing.com'

Key concerns to evaluate:
- File upload security (size validation, file type checking at line 408-420)
- Auth state management
- Memory leaks from timeouts/intervals
- Storage URL handling (line 465)
- Drag and drop implementation
- Error handling patterns
